# ¿Que es un SDK?

Un **SDK** (del inglés **Software Development Kit**) es un **conjunto de herramientas, librerías, documentación y ejemplos de código** que un fabricante o proveedor de software ofrece para que los desarrolladores puedan **crear aplicaciones o integraciones** que funcionen con su plataforma, servicio o producto.

En otras palabras:
👉 Es una **caja de herramientas para desarrolladores**.

---

### 🔧 Componentes típicos de un SDK

Un SDK puede incluir:

* **APIs o librerías**: funciones ya preparadas para interactuar con un sistema (por ejemplo, una API de Google Maps).
* **Herramientas de compilación o depuración**: para compilar código, probarlo y detectar errores.
* **Documentación técnica**: explica cómo usar las funciones o librerías del SDK.
* **Ejemplos de código**: para mostrar cómo implementar ciertas funcionalidades.
* **Emuladores o entornos de prueba**: para simular el comportamiento del sistema sin necesidad de hardware real (por ejemplo, el emulador de Android).

---

### 💡 Ejemplos comunes

* **Android SDK** → Permite crear apps para Android (incluye el emulador, librerías, etc.).
* **AWS SDK** → Permite interactuar con los servicios de Amazon Web Services desde distintos lenguajes (Python, Java, etc.).
* **Google Maps SDK** → Permite integrar mapas en aplicaciones móviles o web.
* **Microsoft Graph SDK** → Permite acceder a datos de Microsoft 365 (como correos, usuarios o calendarios).

---

### 🧩 Diferencia entre SDK y API

* Una **API** es la **interfaz** (conjunto de reglas y funciones) para comunicarse con un sistema.
* Un **SDK** puede incluir **una o más APIs**, además de todo lo necesario para usarlas fácilmente.

👉 En resumen:

> **API** = Qué podés hacer
> **SDK** = Todo lo que necesitás para hacerlo
