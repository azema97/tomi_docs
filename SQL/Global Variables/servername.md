# @@SERVERNAME
Returns the name of the local server that is running SQL Server.

```sql
SELECT @@SERVERNAME AS 'Server Name'
```

```bash
Server Name  
---------------------------------  
name_of_the_server
```